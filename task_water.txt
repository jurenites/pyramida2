________________________________________________________________________________________

Water simulation
Let's implement this suggestion and see where it would lead us. So we need a "water" type of tile that fills in to a certain %. We might not deal width ecivamls and can round it to whole numbers for display mode, or move in 10% steps for visual effect; the 12% of water 
  looks like 1/10 of a cube filled in with very shallow water, and when it's an 89% it looks like a 9/10 deep blue water. again referenaec of Pplorach from a Minecraft toy of water; we probably need a "Falling" water.

We also can add the effect of rain; Minecraft has simple, small #blue #6CAACA rectangles falling down. We also might make a "puddle" look so it gathers on a flat surface, like 1% of water in a cube; we can work in the direct drainage and collecting of water and to be part of a game. And make it work, not exactly fancy physics as the Timber-born game does, but I thing its plausible; we need to add custom_field_parameter in a "water cube": the "stream direction," "stream flow size" (it's a vector, I guess), and how much water is passed by in order to calver aculte ind s can be placed water-wheel. Build and connect it to a mill or a hammer
_________________________________________________________________________________________


by Obbe Vermeij (developer of Plentiful), he replaces traditional frame-by-frame per-tile cellular automata with a Region-Based Lake & Flow Field System [01:52]. This approach allows large lakes to stay perfectly flat instantly without lag or wave propagation delays [01:23]. Below is the complete architectural breakdown and step-by-step implementation guide for your game.1. System Architecture Overview. Instead of running height checks on thousands of individual water tiles every frame [00:38], the system is split into three core components:                         +--------------------------+
                         |     Terrain Mod / Rain   |
                         +------------+-------------+
                                      |
                                      v
+------------------------+   +------------------------+   +------------------------+
|   1. Flow Direction    |-->|   2. Water Particles   |-->|     3. Lake Entities   |
|   Grid (Catchment Map) |   |    (Rain / Downhill)   |   |   (Unified Level Data) |
+------------------------+   +------------------------+   +------------------------+
2. Core Components & Implementation StepsComponent 1: Lake Data Structure (Connected Components)A Lake is treated as a single object containing a collection of connected tiles that all share one uniform water height [02:00].C#public class Lake 
{
    public int id;
    public HashSet<Vector2Int> tiles = new HashSet<Vector2Int>();
    public float totalVolume;   // Volume in cubic units
    public float waterLevel;    // Uniform surface height for ALL tiles [00:02:00]
    
    public void RecalculateLevel() 
    {
        // Simple flat model: level increases equally across all tiles
        if (tiles.Count > 0) 
        {
            waterLevel = baseTerrainHeight + (totalVolume / tiles.Count);
        }
    }
}
Advantage: Updating the water level of a 5,000-tile lake requires modifying one floating-point variable instead of performing 5,000 physics neighbor checks per frame [00:55].Component 2: Flow Direction Map (Steepest Descent / D8 Algorithm)To guide rain droplets and runoff without running physics, maintain a static grid of directional vectors pointing downhill [03:06].Grid Setup: Create a 2D array matching your map resolution: Vector2Int[,] flowDirections.Re-bake on Terrain Edit: Whenever the player digs or places a block, update the flow direction for affected cells [03:15]:$$\text{Flow Direction} = \text{Neighbor tile with the lowest elevation}$$Catchment Areas: Group all tiles whose flow paths lead to the same destination lake [03:53].Component 3: Water Particle Movement (Rain & Streams)Raindrops or stream units move across land using the pre-calculated Flow Map [02:58]:C#public void UpdateRainParticle(RainParticle p) 
{
    Vector2Int currentTile = p.gridPosition;
    
    // Check if particle reached a lake
    if (tileToLakeMap.TryGetValue(currentTile, out Lake lake)) 
    {
        lake.totalVolume += p.volumeAmount;
        lake.RecalculateLevel();
        DestroyParticle(p); // Particle absorbed into lake [00:02:58]
        return;
    }

    // Otherwise, move downhill along the flow map
    Vector2Int nextTile = flowDirections[currentTile.x, currentTile.y];
    p.gridPosition = nextTile;
}
Component 4: Lake Merging, Overflow, and SplittingA. Merging LakesWhen two adjacent lakes rise until their water levels match or submerge the land ridge separating them, merge the smaller lake's tiles and volume into the larger lake [02:08].B. Lake OverflowFind the lowest perimeter land tile (the outlet) bordering the lake [00:30].When lake.waterLevel > outletHeight:Any excess volume above outletHeight spills over the outlet.Spawn water particles at the outlet that travel along the Flow Map to lower basins [00:30].C. Splitting Lakes (Triggered on Drain or Block Placement)Splitting is processor-intensive, so run it only when a player places a wall across a lake or when the water level drops below a terrain saddle point [02:18].C#public void CheckLakeSplit(Lake lake, Vector2Int modifiedTile) 
{
    // Run a Breadth-First Search (BFS) across lake.tiles
    List<HashSet<Vector2Int>> subBasins = FloodFillSubBasins(lake);
    
    if (subBasins.Count > 1) 
    {
        // Split into separate Lake instances [00:02:25]
        foreach (var basinTiles in subBasins) 
        {
            CreateNewLakeFromTiles(basinTiles, lake.totalVolume / subBasins.Count);
        }
        DestroyLake(lake);
    }
}
Component 5: Soil Absorption & Plant Evaporation To keep water levels dynamic and prevent endless map flooding [04:36]: Wet Mud / Absorption: Sand/soil adjacent to a lake absorbs water $\rightarrow$ decreases lake.total Volume while turning the ground into wet mud [04:18]. Plants / Crops: Trees extract moisture from wet mud to grow food [04:26].Evaporators (Reeds): Plant species that absorb excess water at maximum levels and convert it into steam, serving as a natural drain mechanism [04:43].Comparison SummaryFeatureTraditional Cellular AutomataPlentiful's Lake System [01:52]Performance Cost$O(N)$ every frame ($N = \text{water tiles}$) [01:44]$O(1)$ per frame + $O(\text{particles})$Lake LevellingTakes hundreds of frames to equalize [01:23]Instantaneous flat surface [02:00]Rain TransportHeavy grid updatesLightweight vector particle flow [02:58]Terrain EditsAutomatic (slow)Re-bakes local Flow Map & checks BFS split [02:18]Video source: Obbe Vermeij - I Simulated Every Drop of Water in My Game

_________________________________________________________________________________________
1. Voxel Basin Math: Volume to HeightIn a 2D heightmap, the lake's area is constant. In a voxel world, a basin's shape changes at every Y-level. To find the true surface level of a voxel lake, we use the Basin Fill Formula.Let $V$ be the total volume of water. Let $A(y)$ be the accessible surface area (number of empty air blocks) at height $y$. The water fills from the bottom up.The maximum filled height $Y_{surface}$ is the highest level where:$$V \ge \sum_{y=Y_{min}}^{Y_{surface}-1} A(y)$$The fractional height $H_{top}$ of the water blocks at the very top layer is:$$H_{top} = \frac{V - \sum_{y=Y_{min}}^{Y_{surface}-1} A(y)}{A(Y_{surface})}$$This allows your top layer of water blocks to render at a fractional height (like Minecraft's flowing blocks or half-slabs of water) while the blocks below are fully solid.2. Voxel Flow Map GenerationInstead of checking 8 neighbors in 2D, a voxel flow map checks 5 directions, strictly prioritizing gravity.C#using UnityEngine;
using System.Collections.Generic;

public class VoxelFlowField
{
    // Evaluates a 3D coordinate and returns the best downhill direction
    public static Vector3Int GetFlowDirection(Vector3Int pos, VoxelGrid grid)
    {
        // 1. Gravity Priority: If the block below is air, water falls straight down
        if (grid.GetBlock(pos.x, pos.y - 1, pos.z) == BlockType.Air)
        {
            return Vector3Int.down;
        }

        // 2. Lateral Flow: Find the lowest reachable neighbor via Breadth-First Search (BFS)
        // We look for a drop-off (cliff) within a small radius (e.g., 4 blocks)
        Vector3Int bestDrop = FindNearestDropOff(pos, grid, radius: 4);
        
        if (bestDrop != pos)
        {
            // Return the normalized lateral direction toward the drop-off
            return GetDirectionToward(pos, bestDrop);
        }

        // 3. No drop-off found (it's a flat basin) -> flow spreads outward or pools
        return Vector3Int.zero; 
    }

    private static Vector3Int FindNearestDropOff(Vector3Int start, VoxelGrid grid, int radius)
    {
        // Implementation of BFS looking for a block where (Y-1) is Air
        // Omitted for brevity, but standard queue-based pathfinding
        return start; 
    }
}
3. The 3D "Lake" Data StructureWhen falling water hits a bounded area (where GetFlowDirection returns Vector3Int.zero), it converts from a particle/falling block into a VoxelLake entity.C#public class VoxelLake
{
    public int lakeID;
    public float totalVolume; // 1.0f = 1 full block of water
    
    // A dictionary tracking the accessible area (block count) at each Y-level
    public Dictionary<int, int> areaPerYLevel = new Dictionary<int, int>();
    
    // All blocks that make up this specific body of water
    public HashSet<Vector3Int> waterBlocks = new HashSet<Vector3Int>();

    public int minY { get; private set; }
    public int maxY { get; private set; }

    // Calculates which blocks should be full, and which are fractional/surface
    public void RebuildWaterMesh(VoxelGrid grid)
    {
        float remainingVolume = totalVolume;
        int currentY = minY;

        while (remainingVolume > 0 && currentY <= grid.MaxHeight)
        {
            int capacityAtLevel = areaPerYLevel.ContainsKey(currentY) ? areaPerYLevel[currentY] : 0;
            
            if (capacityAtLevel == 0) break; // Reached top of container, overflow happens here

            if (remainingVolume >= capacityAtLevel)
            {
                // Fill this entire Y level (Height = 1.0)
                SetLevelState(currentY, 1.0f, grid);
                remainingVolume -= capacityAtLevel;
            }
            else
            {
                // Fractional top layer (Height = remaining / capacity)
                float fraction = remainingVolume / capacityAtLevel;
                SetLevelState(currentY, fraction, grid);
                remainingVolume = 0;
            }
            currentY++;
        }

        // If remainingVolume > 0 here, the lake overflows!
        if (remainingVolume > 0)
        {
            TriggerOverflow(currentY, remainingVolume, grid);
        }
    }

    private void TriggerOverflow(int overflowY, float excessVolume, VoxelGrid grid)
    {
        // Find the lowest lip of the basin at overflowY
        // Spawn falling water particles/blocks there, subtract excessVolume from this lake
    }
}
4. Merging Lakes in 3DIn a voxel game, lakes merge horizontally AND vertically.Horizontal Merge: If a player mines a block separating two lakes at the same Y-level, their waterBlocks HashSets are combined, and totalVolume is summed.Vertical Merge (Submerging): If Lake A overflows and falls into Lake B (which is in a cave below), Lake A simply transfers volume to Lake B via waterfall particles. They remain separate entities unless Lake B fills all the way up to Lake A's altitude.

_________________________________________________________________________________________


To find the overflow edge (the "lip" of the basin), we need to bridge the lake system with the Flow Map we discussed earlier.

In a voxel basin, an overflow edge is defined as an adjacent Air block where the Flow Direction points outward or downward, pulling water away from the lake. If the adjacent block is just a flat plain that belongs to the same basin, the lake would simply expand its area. It only spills when it hits a block that leads downhill.

Here is the C# logic for finding these edges and converting the excess volume into falling water.

The Overflow Logic
C#
using UnityEngine;
using System.Collections.Generic;

public partial class VoxelLake
{
    // Called when remainingVolume > 0 after filling the current Y-level capacity
    private void TriggerOverflow(int overflowY, float excessVolume, VoxelGrid grid)
    {
        // 1. Find all valid escape points at the current water level
        List<Vector3Int> spillways = FindOverflowEdges(overflowY, grid);

        if (spillways.Count == 0)
        {
            // Edge case: Reached the absolute top of the world with no escape.
            // You can either delete the water, evaporate it, or flood the world ceiling.
            return;
        }

        // 2. Distribute the excess volume evenly among all spillways
        float volumePerSpillway = excessVolume / spillways.Count;

        // 3. Convert excess volume into active falling water / particles
        foreach (Vector3Int spillPoint in spillways)
        {
            SpawnFallingWater(spillPoint, volumePerSpillway, grid);
        }
        
        // 4. Update lake volume to reflect the lost water
        this.totalVolume -= excessVolume;
    }

    private List<Vector3Int> FindOverflowEdges(int overflowY, VoxelGrid grid)
    {
        List<Vector3Int> spillways = new List<Vector3Int>();

        // 4 horizontal neighbors (North, South, East, West)
        Vector3Int[] directions = {
            new Vector3Int(1, 0, 0),
            new Vector3Int(-1, 0, 0),
            new Vector3Int(0, 0, 1),
            new Vector3Int(0, 0, -1)
        };

        // Get the lake's surface blocks at the overflow Y-level
        HashSet<Vector3Int> surfaceBlocks = GetBlocksAtLevel(overflowY);

        foreach (Vector3Int block in surfaceBlocks)
        {
            foreach (Vector3Int dir in directions)
            {
                Vector3Int neighbor = block + dir;

                // We only care about boundaries that are open (Air)
                if (grid.GetBlock(neighbor) == BlockType.Air)
                {
                    // Query the Flow Map: Does this block lead somewhere lower?
                    Vector3Int flowDir = VoxelFlowField.GetFlowDirection(neighbor, grid);

                    // If flowDir is NOT zero, it means gravity pulls water away here.
                    // This is our basin's lip.
                    if (flowDir != Vector3Int.zero)
                    {
                        // Add to spillways (ensure we don't add the same block twice)
                        if (!spillways.Contains(neighbor))
                        {
                            spillways.Add(neighbor);
                        }
                    }
                }
            }
        }

        return spillways;
    }

    // Helper to filter the lake's blocks by Y-level
    private HashSet<Vector3Int> GetBlocksAtLevel(int yLevel)
    {
        HashSet<Vector3Int> blocksAtY = new HashSet<Vector3Int>();
        foreach (Vector3Int block in waterBlocks)
        {
            if (block.y == yLevel)
            {
                blocksAtY.Add(block);
            }
        }
        return blocksAtY;
    }

    private void SpawnFallingWater(Vector3Int position, float volume, VoxelGrid grid)
    {
        // Here you instantiate a dynamic water entity that travels along the Flow Map
        // until it hits a new basin, where it will add its volume to the new lake.
        WaterParticleManager.Spawn(position, volume);
    }
}
How This Behaves in Gameplay
Sheer Cliffs (Waterfalls): If the lake reaches the edge of a cliff, the adjacent block is Air, and the block below it is also Air. The VoxelFlowField returns Vector3Int.down. The code identifies this as a spillway and water falls straight down.

Gentle Slopes (Rivers): If the lake overflows onto a gradual slope, the adjacent block is Air, and the block below it is Solid, but a drop-off exists a few blocks away. The VoxelFlowField returns a horizontal vector pointing toward that drop-off. The code identifies this as a spillway, and the spawned WaterParticle will travel horizontally over the terrain until it falls.

Player Dams: If a player places a dirt block over the spillway, that neighbor is no longer BlockType.Air. The lake will automatically resume filling upward (remainingVolume continues to the next Y-level) until it finds a new, higher spillway.


_________________________________________________________________________________________
The WaterParticleManager acts as the transit system of your water simulation. When a lake overflows, it hands the excess volume to this manager. The manager then moves these "packets" of water block-by-block along the Flow Map until they find a new resting place.

Here is the complete implementation for managing, moving, and merging these dynamic water particles.

1. The Water Particle Data Class
First, we need a lightweight class to track each moving packet of water. It needs to know its volume and have a cooldown timer so water doesn't teleport instantly across the map.

C#
using UnityEngine;

public class WaterParticle
{
    public Vector3Int position;
    public float volume;
    public float moveCooldown; // Controls flow speed
    
    // Optional: Keep track of whether it's falling or flowing laterally for visual effects
    public bool isFalling; 
}
2. The Water Particle Manager
This manager evaluates each active particle every frame (or game tick). It queries the VoxelFlowField to get the next step, and uses a LakeManager to deposit the water when it reaches a basin.

C#
using UnityEngine;
using System.Collections.Generic;

public class WaterParticleManager : MonoBehaviour
{
    public static WaterParticleManager Instance;
    
    private List<WaterParticle> activeParticles = new List<WaterParticle>();
    
    // References to your game's world grids
    private VoxelGrid grid;
    private LakeManager lakeManager;

    // Movement speeds (in seconds per block)
    private readonly float fallSpeed = 0.1f;    // Gravity is fast
    private readonly float flowSpeed = 0.25f;   // Lateral surface flow is slower

    private void Awake()
    {
        Instance = this;
    }

    // Called by VoxelLake.TriggerOverflow()
    public void Spawn(Vector3Int startPosition, float volume)
    {
        activeParticles.Add(new WaterParticle
        {
            position = startPosition,
            volume = volume,
            moveCooldown = flowSpeed
        });
    }

    private void Update()
    {
        float dt = Time.deltaTime;

        // Iterate backwards because we are removing items from the list during the loop
        for (int i = activeParticles.Count - 1; i >= 0; i--)
        {
            WaterParticle p = activeParticles[i];
            p.moveCooldown -= dt;

            // Wait until it's time to move to the next block
            if (p.moveCooldown > 0) continue;

            // 1. Where does gravity/terrain pull this water?
            Vector3Int flowDir = VoxelFlowField.GetFlowDirection(p.position, grid);

            // 2. Basin reached! Water has nowhere else to flow.
            if (flowDir == Vector3Int.zero)
            {
                lakeManager.DepositWater(p.position, p.volume);
                activeParticles.RemoveAt(i);
                continue;
            }

            Vector3Int nextPosition = p.position + flowDir;

            // 3. Did we hit an existing lake mid-flow? (e.g., falling into a lake)
            if (lakeManager.IsLakeBlock(nextPosition))
            {
                lakeManager.DepositWater(nextPosition, p.volume);
                activeParticles.RemoveAt(i);
                continue;
            }

            // 4. Move the particle
            p.position = nextPosition;
            
            // Set speed based on direction
            p.isFalling = (flowDir.y < 0);
            p.moveCooldown = p.isFalling ? fallSpeed : flowSpeed;
            
            // (Optional) Update visual representation (like moving a mesh or particle effect)
            UpdateParticleVisuals(p);
        }
    }

    private void UpdateParticleVisuals(WaterParticle p)
    {
        // Link this logic to Unity's ParticleSystem, or spawn a temporary "Flowing Water" 
        // voxel block at p.position that lasts for a fraction of a second.
    }
}
3. Bridging to the Lake Manager
To make the integration complete, your system needs a central authority that knows where all resting water is. When lakeManager.DepositWater(pos, volume) is called, it does one of two things:

Expands an Existing Lake: If pos is adjacent to an existing VoxelLake, it adds the volume to that lake and calls lake.RecalculateLevel().

Creates a New Lake: If pos is totally dry, it instantiates a brand new VoxelLake with 1 block of area and the deposited volume.

Design Tip: To prevent a "trickle" effect where thousands of tiny 0.01 volume particles clog your CPU, you can implement a minimum volume threshold in the Spawn method. If the excess volume is too small, round it up to a minimum chunk size and spawn fewer particles. it s a 10% . ytereshhold. and puddle look for a 1%  water . containing almost negletable amount ow water.

_________________________________________________________________________________________


Here is the complete C# implementation for LakeManager.DepositWater.It evaluates whether the landing position touches existing water bodies, expands single lakes, or bridges and merges multiple lakes together when water fills the gaps between them.C#using UnityEngine;
using System.Collections.Generic;
using System.Linq;

public class LakeManager : MonoBehaviour
{
    public static LakeManager Instance;

    [SerializeField] private VoxelGrid grid;

    // Fast lookup for finding which lake owns any given 3D block coordinate
    private Dictionary<Vector3Int, VoxelLake> blockToLakeMap = new Dictionary<Vector3Int, VoxelLake>();
    private List<VoxelLake> activeLakes = new List<VoxelLake>();
    
    private static readonly Vector3Int[] OrthogonalDirections = {
        new Vector3Int(1, 0, 0),  // East
        new Vector3Int(-1, 0, 0), // West
        new Vector3Int(0, 1, 0),  // Up
        new Vector3Int(0, -1, 0), // Down
        new Vector3Int(0, 0, 1),  // North
        new Vector3Int(0, 0, -1)  // South
    };

    private void Awake()
    {
        Instance = this;
    }

    public bool IsLakeBlock(Vector3Int pos)
    {
        return blockToLakeMap.ContainsKey(pos);
    }

    /// <summary>
    /// Deposits water volume at a resting position and manages lake creation/expansion/merging.
    /// </summary>
    public void DepositWater(Vector3Int position, float volume)
    {
        // 1. Direct Hit: If the position is already inside an existing lake block
        if (blockToLakeMap.TryGetValue(position, out VoxelLake existingLake))
        {
            existingLake.totalVolume += volume;
            existingLake.RebuildWaterMesh(grid);
            return;
        }

        // 2. Check for surrounding lakes (6 orthogonal directions)
        HashSet<VoxelLake> adjacentLakes = GetAdjacentLakes(position);

        if (adjacentLakes.Count == 0)
        {
            // SCENARIO A: No nearby water -> Create a brand new lake
            CreateNewLake(position, volume);
        }
        else if (adjacentLakes.Count == 1)
        {
            // SCENARIO B: Touches 1 lake -> Expand it
            VoxelLake targetLake = adjacentLakes.First();
            AddBlockToLake(targetLake, position);
            targetLake.totalVolume += volume;
            targetLake.RebuildWaterMesh(grid);
        }
        else
        {
            // SCENARIO C: Touches 2+ lakes -> Bridge and merge them into one
            VoxelLake primaryLake = SelectPrimaryLake(adjacentLakes);

            // Add the bridge block and volume to the primary lake
            AddBlockToLake(primaryLake, position);
            primaryLake.totalVolume += volume;

            // Merge all secondary lakes into the primary lake
            foreach (VoxelLake secondaryLake in adjacentLakes)
            {
                if (secondaryLake != primaryLake)
                {
                    MergeLakes(primaryLake, secondaryLake);
                }
            }

            primaryLake.RebuildWaterMesh(grid);
        }
    }

    private void CreateNewLake(Vector3Int position, float volume)
    {
        VoxelLake newLake = new VoxelLake
        {
            lakeID = System.Guid.NewGuid().GetHashCode(),
            totalVolume = volume
        };

        activeLakes.Add(newLake);
        AddBlockToLake(newLake, position);
        newLake.RebuildWaterMesh(grid);
    }

    private void AddBlockToLake(VoxelLake lake, Vector3Int position)
    {
        if (lake.waterBlocks.Add(position))
        {
            blockToLakeMap[position] = lake;

            // Track surface area capacity per Y-level
            int y = position.y;
            if (!lake.areaPerYLevel.ContainsKey(y))
            {
                lake.areaPerYLevel[y] = 0;
            }
            lake.areaPerYLevel[y]++;
        }
    }

    private HashSet<VoxelLake> GetAdjacentLakes(Vector3Int pos)
    {
        HashSet<VoxelLake> neighborLakes = new HashSet<VoxelLake>();

        foreach (Vector3Int dir in OrthogonalDirections)
        {
            Vector3Int neighborPos = pos + dir;
            if (blockToLakeMap.TryGetValue(neighborPos, out VoxelLake lake))
            {
                neighborLakes.Add(lake);
            }
        }

        return neighborLakes;
    }

    private VoxelLake SelectPrimaryLake(HashSet<VoxelLake> lakes)
    {
        // Pick the largest lake as the primary destination to minimize updates during merges
        return lakes.OrderByDescending(l => l.waterBlocks.Count).First();
    }

    private void MergeLakes(VoxelLake primary, VoxelLake secondary)
    {
        // 1. Combine total volumes
        primary.totalVolume += secondary.totalVolume;

        // 2. Transfer block ownership and update area counts
        foreach (Vector3Int block in secondary.waterBlocks)
        {
            primary.waterBlocks.Add(block);
            blockToLakeMap[block] = primary; // Retarget lookup dictionary to primary

            int y = block.y;
            if (!primary.areaPerYLevel.ContainsKey(y))
            {
                primary.areaPerYLevel[y] = 0;
            }
            primary.areaPerYLevel[y]++;
        }

        // 3. Remove secondary lake from memory
        activeLakes.Remove(secondary);
    }
}
Key Highlights of This LogicO(1) Direct Lookup: Using blockToLakeMap lets the particle manager determine in constant time ($O(1)$) whether water hits an existing lake.Seamless Merging: When water accumulates between two separate pools (like filling a ditch between two ponds), MergeLakes transfers all blocks and volume into the largest pool (primary), automatically updating map lookup references.Dynamic Area Capacity Tracking: AddBlockToLake updates areaPerYLevel in real-time so lake.RebuildWaterMesh() knows the exact cross-sectional surface area at every height level.

_________________________________________________________________________________________

Visual Water 1

Godot 4 Water Shader CodeCreate a new shader file named voxel_water.gdshader:Code snippetshader_type spatial;
render_mode depth_draw_always, cull_back;

// Water Color Settings
uniform vec4 shallow_color : source_color = vec4(0.15, 0.55, 0.82, 0.6);
uniform vec4 deep_color : source_color = vec4(0.02, 0.12, 0.35, 0.9);
uniform float depth_distance : hint_range(0.1, 20.0) = 3.0;

// Refraction / Distortion
uniform sampler2D noise_texture : hint_default_white, filter_nearest;
uniform float distortion_scale : hint_range(0.0, 0.05) = 0.015;
uniform float distortion_speed : hint_range(0.0, 2.0) = 0.2;

// Shoreline Foam
uniform vec4 foam_color : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float foam_thickness : hint_range(0.0, 1.0) = 0.25;

// Godot 4 Built-in Texture Samplers
uniform sampler2D SCREEN_TEXTURE : hint_screen_texture, filter_nearest;
uniform sampler2D DEPTH_TEXTURE : hint_depth_texture, filter_nearest;

void fragment() {
    // 1. Get world depth of geometry behind water
    float raw_depth = texture(DEPTH_TEXTURE, SCREEN_UV).r;
    vec3 ndc = vec3(SCREEN_UV * 2.0 - 1.0, raw_depth);
    vec4 view_coords = INV_PROJECTION_MATRIX * vec4(ndc, 1.0);
    view_coords.z /= view_coords.w;
    
    float ground_depth = -view_coords.z;
    float water_surface_depth = -VERTEX.z;

    // Depth difference (how deep the water is at this pixel)
    float depth_diff = ground_depth - water_surface_depth;
    float depth_fade = clamp(depth_diff / depth_distance, 0.0, 1.0);

    // 2. Underwater Refraction (Noise distortion offset)
    vec2 noise_uv = UV + vec2(TIME * distortion_speed, TIME * distortion_speed * 0.5);
    vec2 distortion = (texture(noise_texture, noise_uv).rg * 2.0 - 1.0) * distortion_scale;
    
    // Scale down distortion near the surface
    distortion *= depth_fade;

    // Sample distorted underwater scene color
    vec2 distorted_uv = SCREEN_UV + distortion;
    vec3 underwater_color = texture(SCREEN_TEXTURE, distorted_uv).rgb;

    // 3. Blend Shallow -> Deep Water
    vec4 water_tint = mix(shallow_color, deep_color, depth_fade);
    vec3 final_color = mix(underwater_color, water_tint.rgb, water_tint.a);

    // 4. Shoreline Foam Mask
    if (depth_diff > 0.0 && depth_diff < foam_thickness) {
        float foam_factor = 1.0 - (depth_diff / foam_thickness);
        final_color = mix(final_color, foam_color.rgb, step(0.5, foam_factor));
    }

    ALBEDO = final_color;
    ROUGHNESS = 0.1;
    METALLIC = 0.0;
}
How to Set It Up in Godot 4Material Creation:Create a ShaderMaterial on your water mesh or voxel block faces.Assign voxel_water.gdshader to the material.Noise Texture Assignment:In the material inspector, assign a FastNoiseLite texture to the noise_texture slot.Set its noise type to Simplex or Perlin, and ensure the texture import/sampler filtering uses Nearest (handled automatically by filter_nearest in code).Pixelated Aesthetic Configuration:Using filter_nearest on SCREEN_TEXTURE and DEPTH_TEXTURE keeps pixels crisp rather than blurry when sampling underwater objects.For a full retro voxel look, set your project viewport resolution lower (e.g., $480 \times 270$ or $640 \times 360$) and set stretch/mode to viewport in Project Settings -> Window.Key Voxel Adaptation DetailsNo Waves / Flat Geometry: The vertex function is completely empty, ensuring block faces align flush with adjacent voxel blocks.Step Foam (step(0.5, foam_factor)): Replaces smooth foam gradients with hard pixelated line edges that wrap around Minecraft-style block shores.
_________________________________________________________________________________________
Visual Water 2.

Planar Architecture vs. Volumetric Water [01:10]Concept: Instead of building expensive, fully 3D interactive volumetric water geometry, use a 2D planar mesh acting as a "canvas" for shader post-processing.Why: Volumetric liquid simulation is computationally heavy and often unnecessary unless core gameplay mechanics require physical 3D fluid interaction.Depth-Based Absorption & Color Tinting [02:05]Physics: Real water absorbs and scatters light as depth increases, making deep underwater objects darker and desaturated.Implementation:Sample the scene depth texture beneath the water plane.Calculate a depth fade value ranging from 0.0 (surface) to 1.0 (maximum depth).Use linear interpolation (lerp) between a shallow water color and a deep water color based on the depth fade value.Pixel Art Note: Combine depth calculations with color quantization to prevent smooth gradients from breaking the low-resolution pixel aesthetic.Underwater Refraction & Distortion [03:50]Concept: Fake the bending of light through water without expensive physical raytracing or complex high-poly geometry simulation.Implementation:Generate an animated noise texture (scrolling UV coordinates over time).Use the noise texture to offset the UV coordinates when sampling the underwater screen/scene color.Multiply the distortion offset by the depth fade value so objects near the surface remain crisp while deeper objects experience stronger wavy distortion.Macro Waves via Vertex Displacement [05:08]Concept: Give the flat plane physical movement without overwhelming the renderer.Implementation:Performed in the vertex shader on a low-resolution subdivided plane mesh.Calculate two perpendicular sine waves: one along the primary wave axis and another rotated 90° around the Y-axis.Multiply the two waves together using time, speed, and direction parameters to produce an organic ebb-and-flow motion.Shoreline & Surface Foam [06:41]Shore Foam Detection: Compare the depth of the underwater ground against the depth of the water surface. Where the depth difference is very small (near 0.0), geometry is intersecting the shore. Mask a 1-pixel thick foam band or texture at this intersection.Surface Whitecaps: Sample a scrolling noise texture on top of the water plane. Apply a hard threshold cutoff to isolate peak values and composite animated white foam highlights across the surface.Layer Compositing Stack [08:06]The shader composites layers in the following order:Refracted / Distorted Scene Color (Base)Watercolor Depth Gradient (lerp shallow $\to$ deep)Shoreline Foam MaskDetail Noise & Procedural Surface Foam (Whitecaps)Planar ReflectionsPlanar Reflections & Secondary Camera Matrix [08:51]Approach: Screen-space reflections (SSR) miss off-screen details, and cubemaps lack sharp geometry reflection. Planar reflection is used instead.Implementation:Define the water surface as a world-space plane equation: $Ax + By + Cz + D = 0$.Construct a reflection matrix that mirrors space across this plane.Create a secondary reflection camera (using a lightweight render pipeline for performance) that copies the main camera's transform, applies the reflection matrix, and stays locked frame-by-frame.Project the reflection render texture onto the water plane in the shader and blend it based on viewing angle (Fresnel effect).2. Adapting This Look to a Minecraft-Style 3D Voxel GameWhen applying this water pipeline to a voxel world (Minecraft-like terrain) in 3rd-person view, consider these adaptations:1. Handling Voxel Block GeometryTop Planes vs. Side Faces: In voxel terrain, water isn't just one giant flat ocean plane; it flows across stepped block heights. Apply the main vertex displacement and reflection pass primarily to top horizontal water faces.Voxel Shore Detection: Standard depth-difference shore foam works automatically when water blocks intersect solid ground blocks. Reduce the depth-fade threshold so foam highlights cling tightly to voxel edges.2. Pixelation & Resolution LockPixel Grid Quantization: To keep 3D water reflections and refraction pixel-perfect alongside blocky terrain, render the entire camera view (or reflection pass) to a pixelated off-screen render target (e.g., $320 \times 180$ or $640 \times 360$) using point/nearest-neighbor filtering.Color Palette Quantization: Quantize the depth gradient and reflection blending so colors snap to discrete visual steps rather than smooth 32-bit gradients.3. 3rd-Person Camera AdjustmentsReflection Camera Alignment: Since your camera orbits the player character in 3rd-person, ensure the secondary reflection camera updates every frame relative to the main camera's position and orientation.Player Character Reflection: Include the 3rd-person player model in the reflection camera’s cull mask so the character casts a real-time pixelated reflection onto the water surface.
_________________________________________________________________________________________

_________________________________________________________________________________________
 a water trail effect in a godot
https://www.youtube.com/watch?v=W4eVR_Fm5Gs
